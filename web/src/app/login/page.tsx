import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function LoginPage() {
  return (
    <PlaceholderPage
      kicker="Access"
      title="SIGN IN"
      body="Whop OAuth is not in this pass. This route stands in for auth-callback.html. Later it will hand off to the existing spark-auth flow."
    />
  );
}
