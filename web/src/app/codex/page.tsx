import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function CodexPage() {
  return (
    <PlaceholderPage
      kicker="Commons"
      title="CODEX"
      body="Lore, commons, and zone field notes. This route replaces commons.html when the OS is the host. Content stays on the live HTML doorway until then."
    />
  );
}
