import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function DashboardPage() {
  return (
    <PlaceholderPage
      kicker="Player home"
      title="DASHBOARD"
      body="Signed-in home for Players. Auth and Whop session wiring come in a later pass. Sign in is a button only in Phase 1."
    />
  );
}
