import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function GatePage() {
  return (
    <PlaceholderPage
      kicker="Threshold"
      title="GATE"
      body="Membership gate placeholder. Tools stay on their current HTML URLs until this shell is the host and spark-auth is wired in."
    />
  );
}
