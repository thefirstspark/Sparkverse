import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function VaultPage() {
  return (
    <PlaceholderPage
      kicker="Holdings"
      title="VAULT"
      body="Artifacts, receipts, and soul-map records will live here. Phase 1 is the shell only — no auth, no inventory yet."
    />
  );
}
